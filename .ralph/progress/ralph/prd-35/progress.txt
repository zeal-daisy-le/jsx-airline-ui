Issue #18 — Seat map step (2026-05-26)

Implemented the seat map booking step at `/booking/seats`. Created the BFF `GET /api/booking/seatmap` endpoint that returns a mock 15-row × 6-column (A–F) aircraft layout with first class rows 1–3 ($45/seat) and economy rows 4–15 (included), with 11 pre-occupied seats baked in, and `POST /api/booking/seatmap` with Zod validation on the `assignments` array (passengerIndex + seatNumber regex `/^\d{1,2}[A-F]$/`, max 9 entries) that stubs the Navitaire session-update call (pending issue #5). Replaced the stub `pages/booking/seats.tsx` with a full implementation: a visual cabin grid rendered row-by-row with an aisle gap between columns C and D, class-section dividers, a colour-coded legend (available/selected/occupied/first-class), and per-seat buttons carrying descriptive `aria-label` (row, seat, class, price, availability status) and `aria-pressed`. For multi-passenger bookings, passenger-selector tabs show above the map — clicking a tab changes the active passenger; selecting a seat auto-advances to the next unassigned passenger. Seat states cover: available (white/amber for first class), selected for current passenger (jsx-red, aria-pressed=true), held by another passenger (gray, disabled), and occupied (gray, disabled). A `seat-progress` indicator with `aria-live="polite"` tracks how many passengers are seated. The Continue button is enabled only when all passengers have a seat; a separate "Skip seats" button and a "Skip seat selection" header link both call `setSeatAssignments([])` (marking the step valid) and navigate to `/booking/review` without calling the BFF. On Continue, `withRetry` wraps the POST; on success `setSeatAssignments` saves to the Zustand store, GA4 `booking_step_completed` fires, and the user navigates to `/booking/review`. Load failures use a plain `fetch` with `showToast` and an inline "Try again" button. Store pre-fill restores previous assignments on back-navigation. Added 57 Vitest tests (BFF GET/POST validation, page rendering, multi-passenger tabs, seat selection, aria-labels, pre-fill, skip flows, successful submission, BFF error recovery, load error recovery, GA4 events). All 430 tests pass.

---

Issue #16 — Traveller details step (2026-05-22)

Implemented the traveller details booking step at `/booking/details`. Extended `stores/bookingStore.ts` by adding `nationality` to the `TravelerInfo` interface and introducing a new `ContactDetails` type (`{ email, phone }`) with a corresponding `contactDetails` store field and `setContactDetails` action — contact details are persisted to sessionStorage but never server-logged. Created the BFF `POST /api/booking/details` endpoint (`pages/api/booking/details.ts`) with Zod validation on all passenger and contact fields; the endpoint stubs the Navitaire session-update call (pending issue #5) and never logs document numbers. Built the full multi-passenger form in `pages/booking/details.tsx` using React Hook Form + Zod (`mode: "onBlur"` for inline per-field validation) with `useFieldArray`-style indexed registration; one fieldset is rendered per passenger (adult/child/infant) with fields for first name, last name, date of birth, document type (passport/national ID radio), document number, and nationality, plus a shared contact details fieldset. On mount the page fetches `/api/auth/me` and pre-fills the first passenger's name and the contact email from the logged-in user profile if those fields are currently empty. On submit the BFF is called via `withRetry`, then `setTravelerInfo` and `setContactDetails` are saved to the store, GA4 `booking_step_completed` fires, and the user is navigated to `/booking/bags`. All inputs have explicit `<label>` elements, `aria-required`, `aria-describedby` wired to error messages, and error messages use `role="alert"`. Added 32 Vitest tests (BFF validation, page rendering, multi-passenger layout, auth pre-fill, store pre-fill, blur validation, successful submit, BFF error recovery, GA4 events, accessibility). All 308 tests pass.

---

Issue #15 — Passenger selection step (2026-05-22)

Implemented the full passenger selection booking step at `/booking/passengers`. Created a reusable `PassengerStepper` component (`components/booking/PassengerStepper.tsx`) that renders accessible +/− stepper controls with `role="group"`, `aria-label` on each button, `aria-live="polite"` on the count output, and disabled states at min/max — satisfying WCAG 2.1 AA. The page (`pages/booking/passengers.tsx`) pre-fills adult/child/infant counts from the Zustand store on rehydration, enforces the min-1-adult and infants-≤-adults constraints both at the stepper UI level (max prop) and in the submit handler, auto-caps the infant count when adults are reduced, fires `bookingEvents.stepViewed` and `bookingEvents.stepCompleted` GA4 events, saves selections to the Zustand store via `setPassengers` on Continue (triggering downstream step invalidation), and navigates to `/booking/details`. 29 unit tests were written covering the stepper component in isolation and the full page, all 276 tests in the suite pass.

---

Issue #14 — Flight search step (GET /api/search BFF + search UI)

Implemented the complete flight search step as the first interactive booking step. Added BookingFlowRouter infrastructure from the stalled issue-12 branch: `lib/booking/steps.ts` (step ordering, accessibility helpers), `hooks/useBookingGuard.ts` (step guard with sessionStorage hydration waiting), `components/booking/StepProgress.tsx` (accessible progress indicator), `components/booking/BookingLayout.tsx` (shared booking wrapper with noindex meta), and scaffold pages for all 8 booking steps. Updated `stores/bookingStore.ts` to add `hasHydrated` (with `onRehydrateStorage` persist hook) and `bookingReference` fields required by the guard and confirmation page. Implemented the BFF `GET /api/search` endpoint with Zod input validation, mock data generation (used when `NAVITAIRE_API_URL` is unset), and Navitaire proxy skeleton (with a TODO referencing issue #5). Built the flight search UI in `pages/booking/flights.tsx` using React Hook Form + Zod for validation, a touch-friendly date input, per-type passenger counters, loading skeletons, flight result cards with price rollup and low-availability warnings, GA4 `booking_step_viewed`/`booking_step_completed` events, and `withRetry`-backed error recovery via `useErrorStore`. Added 49 new Vitest tests (steps utilities, BFF endpoint validation, mock and proxy modes, `FlightResultCard` rendering and interaction, bookingStore `hasHydrated` and `bookingReference`). All 245 tests pass.

---

Issue #17 — Bags selection step (2026-05-26)

Implemented the bags selection booking step at `/booking/bags`. Created the BFF `GET /api/booking/bags` endpoint that returns mock bag options (0/1/2 checked bags with prices $0/$30/$55) with `BagOption` type export, and `POST /api/booking/bags` with Zod validation on the `selections` array (passengerIndex + checkedBags 0–2, max 9 entries) that stubs the Navitaire session-update call (pending issue #5). Replaced the stub `pages/booking/bags.tsx` with a full implementation: per-passenger `<fieldset>` sections with radio-button option cards styled to highlight the selected option, a real-time running total panel with `aria-live="polite"`, loading skeletons (aria-hidden) while bag options load, and inline error recovery (alert + "Try again" button) when the GET fails — `showToast` is called and the Continue button remains accessible so users can skip bags entirely. On submit, `withRetry` wraps the POST to `/api/booking/bags`, and on success `setBagSelections` saves to the Zustand store, GA4 `booking_step_completed` fires, and the user navigates to `/booking/seats`. Store pre-fill restores previous selections on back-navigation. Established the pattern that `withRetry` is for *submit* mutations only — data loading uses a plain `fetch` with a manual UI retry button to avoid blocking the UI with backoff delays. Added 34 Vitest tests (BFF GET/POST validation, page rendering, multiple passengers, running total, pre-fill, zero-bag submit, successful submission, BFF error recovery, load error recovery, GA4 events, accessibility). All 373 tests pass.

---

Issue #19 — Booking review step (2026-05-26)

Implemented the booking review step at `/booking/review` — a read-only order summary displayed before payment. Created the BFF `POST /api/booking/confirm-price` endpoint with Zod validation (`flightId`, `totalPassengers 1–9`, `previousTotalPrice ≥ 0`) that re-confirms the booking price with Navitaire before charging (stubbed pending issue #5); the mock always confirms at the submitted price. Replaced the stub `pages/booking/review.tsx` with a full implementation: four section cards (Flight, Passengers, Bags, Seats) each with an "Edit" link back to the respective step, descriptive `aria-label` on all "Edit" links and list elements. Bag option prices are fetched from `GET /api/booking/bags` on mount (plain fetch, no retry) to compute the price breakdown; load failure shows "Price unavailable" with an inline "Try again" button and calls `showToast`. Seat prices are derived from the seat number: rows 1–3 = $45 (first class), rows 4+ = $0 (economy). A price breakdown panel shows base fare, bags, seat fees, estimated taxes (12%), and total, with a loading skeleton while bag options are loading. On "Confirm & pay": `POST /api/booking/confirm-price` is called with `withRetry`; if `confirmed: false` is returned, a price-change banner replaces the CTA button showing the old and new prices — the user must accept (navigating to `/booking/payment`) or cancel to stay on review. On confirmed response: `markStepValid("review")` is called, `bookingEvents.stepCompleted` fires with `{ totalPrice }`, and the user navigates to `/booking/payment`. Added 72 Vitest tests (BFF validation, page rendering, flight/passenger/bag/seat sections, price computation, edit links, confirm-pay flow, price-change dialog, BFF error recovery, load error recovery, GA4 events, accessibility). All 502 tests pass.

Issue #25 — Lighthouse CI + performance budget gates (2026-05-26)

Implemented Lighthouse CI performance gates for every PR. Created `.lighthouserc.js` at the repo root configuring `@lhci/cli` to run 3 mobile-simulated Lighthouse audits against two URLs — the homepage (`/`) and the first booking step (`/booking/flights`) — with hard budget assertions: mobile Performance score ≥ 90, LCP ≤ 2.5 s, CLS ≤ 0.1. The base URL is read from `LHCI_BASE_URL` env var (fallback `http://localhost:3000`), throttled to simulate a mid-tier 4G mobile connection. Created `.github/workflows/lighthouse.yml` which triggers on PRs to main and: (1) polls the GitHub Deployments API for up to 10 minutes until Vercel posts a successful preview deployment status with an `environment_url`; (2) installs `@lhci/cli@0.14.x` globally and runs `lhci autorun` against the preview URL using the config; (3) parses the per-run JSON reports from `.lighthouseci/` and posts a markdown score-breakdown table as a PR comment (or updates the existing bot comment on re-runs); (4) re-fails the job if LHCI reported assertion violations, blocking the PR merge via required status checks. If no preview URL is found within 10 minutes, a skip notice is posted instead and the job exits cleanly. The optional `LHCI_GITHUB_APP_TOKEN` secret enables richer GitHub App status annotations without being required. All 502 existing tests continue to pass.

---

Issue #21 — Booking confirmation + post-booking account prompt (2026-05-26)

Implemented the booking confirmation step at `/booking/confirmation`. Added `confirmedTotalPrice: number | null` to the Zustand booking store (persisted to sessionStorage) and updated the review page to call `setConfirmedTotalPrice(total)` before navigating to payment. Created the BFF `POST /api/booking/send-confirmation` endpoint with Zod validation (`bookingReference`, `contactEmail`, `passengerCount 1–9`, `flightId`) that stubs confirmation email dispatch via Navitaire (pending issue #5). Replaced the stub confirmation page with a full implementation using a snapshot-then-clear pattern: on mount, all booking data is captured into local React state from `useBookingStore.getState()`, then `trackEvent("booking_completed")` fires with `booking_reference`, `total_value`, and `passenger_count`, `POST /api/booking/send-confirmation` is called fire-and-forget (failing silently with a toast), and `resetBooking()` clears the Zustand store and sessionStorage. The page renders entirely from the snapshot — no store subscriptions after mount — so clearing does not blank the display. The confirmation screen shows: a PNR card with `aria-label="Booking reference: <ref>"` for screen reader announcement, flight origin/destination/date, a passenger list, seat assignments (if present), and a total paid panel. Guest users see a non-blocking account creation prompt with a "No thanks" dismiss button; logged-in users see an account association notice. Auth status is determined via `GET /api/auth/me`. A "Print confirmation" button calls `window.print()`; the SiteHeader and action bar are hidden in print media via `print:hidden`. Added 66 Vitest tests (15 BFF validation/success tests, 51 page tests covering guard redirect, PNR rendering + aria, flight/passenger/seat/price sections, account prompt/dismiss, account association, store clearing, GA4 events, email BFF call, print button, auth failure fallback). All 568 tests pass.

---

Issue #24 — Playwright E2E critical path + axe-core accessibility CI (2026-05-26)

Implemented end-to-end Playwright tests and axe-core accessibility CI for the full booking funnel. Added `@axe-core/playwright` to devDependencies and created `playwright.config.ts` (two projects: Desktop Chrome + Pixel 5, sequential workers, mock NAVITAIRE_API_URL so BFF always returns deterministic data, `next dev` locally / `next start` in CI). Created `e2e/helpers/booking.ts` with a `seedBookingStateUpTo` helper that writes a Zustand persist payload into `sessionStorage` so specs can start mid-flow without replaying all prior steps via the UI. Wrote five test files: `e2e/booking-flow.spec.ts` (full guest critical path: flights search → flight select → passengers → traveller details → bags → seats skip → review → payment mock → confirmation with PNR assertion), `e2e/back-navigation.spec.ts` (navigate forward 3 steps, go back 2, verify form field values retained from sessionStorage), `e2e/api-failure.spec.ts` (three tests using `page.route()` to simulate BFF failures on search, bag load, and seat map load — verifying toast/inline error and continue accessibility), `e2e/accessibility.spec.ts` (axe-core WCAG 2.1 AA scan on homepage and all 7 booking steps using seeded store state), `e2e/auth-prefill.spec.ts` (three tests: pre-fill from auth/me, no-overwrite when data already set, empty form for guests), `e2e/session-recovery.spec.ts` (three tests skipped with `test.skip(true, '...')` pending issue #13). Created `.github/workflows/playwright.yml` that builds Next.js, installs Playwright browsers, runs `npx playwright test` in CI mode, and uploads the HTML report as an artifact (videos/traces uploaded on failure). Added `POST /api/booking/pay` BFF stub (`pages/api/booking/pay.ts`, Zod-validated, returns mock PNR + paymentToken, TODO for #20 gateway integration) and enhanced `pages/booking/payment.tsx` from a stub to a working page with a "Pay now" button (`data-testid="pay-button"`) that calls the BFF, sets `paymentToken` + `bookingReference` in the store, fires GA4 `booking_step_completed`, and navigates to confirmation — completing the E2E flow. Excluded `e2e/**` from Vitest via `vitest.config.ts`. All 568 existing Vitest tests continue to pass.

---
